# Context Package: Three.js Minimal Starter Project

## Codebase References

### Files Currently Present in Repository

| File Path | Purpose | Status | Action Required |
|-----------|---------|--------|-----------------|
| `./package.json` | npm dependencies and scripts | ✅ Exists and compliant | **Validate only** — confirmed compliant in Intent Document |
| `./README.md` | Project documentation | ✅ Exists (minimal) | **Preserve** — contains "Testing purposes" placeholder |
| `./index.html` | HTML entry point | ✅ Exists | **Validate and conditionally regenerate** — must verify compliance with Intent requirements |
| `./src/main.js` | Three.js scene implementation | ✅ Exists | **Validate and conditionally regenerate** — must verify ≤100 lines, correct imports, complete scene |
| `./src/style.css` | Viewport CSS reset | ✅ Exists | **Validate and conditionally regenerate** — must verify full-viewport coverage without scrollbars |

### ORBITAL Metadata (Do Not Touch)

| Directory Path | Purpose |
|----------------|---------|
| `./.orbital/artifacts/c71c2625-0f6b-441c-a24b-a5d187d1ae16/*` | Prior orbit artifacts (6 files: intent, context, proposal, verification, code_generation, test_results) |
| `./.orbital/artifacts/a6b4c09a-283b-48f1-9386-68f4ef295656/*` | Current orbit artifacts in progress (intent, context, proposal generated so far) |

**Critical Constraint:** Do not read, modify, or delete any files under `.orbital/`. These are system metadata managed by the ORBITAL framework.

### Existing `package.json` Validation

**Current Content:**
```json
{
  "name": "threejs-starter",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
```

**Compliance Status:**
- ✅ `"type": "module"` — Enables ES6 imports in Node.js context (required for Vite + Three.js)
- ✅ `"scripts.dev": "vite"` — Matches Intent requirement for dev script
- ✅ `"dependencies.three": "^0.160.0"` — Meets Intent requirement for latest stable Three.js
- ✅ `"devDependencies.vite": "^5.0.0"` — Meets Intent requirement for Vite 5.x
- ✅ `"engines.node": ">=18.0.0"` — Enforces Vite 5.x runtime requirement

**Conclusion:** Existing `package.json` is fully compliant with Intent constraints. **No modifications required** — file will be preserved as-is.

### Files Requiring Validation

The orbit must read and validate three implementation files generated by orbit c71c2625:

**`./index.html` Validation Checklist:**
- [ ] DOCTYPE declaration present (`<!DOCTYPE html>`)
- [ ] `<html lang="en">` attribute set
- [ ] `<meta charset="UTF-8">` tag present
- [ ] `<meta name="viewport" content="width=device-width, initial-scale=1.0">` present
- [ ] `<title>` tag contains descriptive text
- [ ] `<link rel="stylesheet" href="/src/style.css">` in `<head>`
- [ ] `<canvas id="app"></canvas>` in `<body>`
- [ ] `<script type="module" src="/src/main.js"></script>` in `<body>` after canvas
- [ ] No additional elements beyond minimal structure

**`./src/main.js` Validation Checklist:**
- [ ] Line count ≤ 100 (including comments and whitespace)
- [ ] Import Three.js core: `import * as THREE from 'three';`
- [ ] Import OrbitControls: `import { OrbitControls } from 'three/examples/jsm/controls/OrbitControls.js';` (note `/jsm/` path and `.js` extension)
- [ ] Scene created with background color `0x1a1a2e`
- [ ] PerspectiveCamera with FOV between 45-75° (suggest 75°)
- [ ] Camera positioned at Z=5 or similar distance
- [ ] WebGLRenderer attached to `document.querySelector('#app')`
- [ ] Renderer size set to window dimensions
- [ ] Renderer pixel ratio set to device pixel ratio
- [ ] BoxGeometry(1, 1, 1) created
- [ ] MeshStandardMaterial created (not MeshBasicMaterial)
- [ ] Mesh created from geometry + material and added to scene
- [ ] AmbientLight added to scene with intensity ~0.5
- [ ] DirectionalLight added to scene with intensity ~0.5, positioned away from origin
- [ ] OrbitControls instantiated with camera and renderer.domElement
- [ ] `controls.enableDamping = true` set
- [ ] Animation function using `requestAnimationFrame`
- [ ] Cube rotation on both X and Y axes in animation loop
- [ ] `controls.update()` called in animation loop
- [ ] `renderer.render(scene, camera)` called in animation loop
- [ ] Window resize event listener registered
- [ ] Resize handler updates `camera.aspect` to new ratio
- [ ] Resize handler calls `camera.updateProjectionMatrix()`
- [ ] Resize handler calls `renderer.setSize()` with new dimensions

**`./src/style.css` Validation Checklist:**
- [ ] Universal selector reset: `* { margin: 0; padding: 0; box-sizing: border-box; }`
- [ ] Full-height HTML/body: `html, body { width: 100%; height: 100%; overflow: hidden; }`
- [ ] Canvas display override: `#app { display: block; width: 100%; height: 100%; }`
- [ ] No extra rules beyond these 3 core selectors

## Architecture Context

### Project Type: Validation and Conditional Regeneration

This orbit differs from orbit c71c2625 in approach. Rather than generating files from scratch, this orbit:
1. **Reads existing files** from prior orbit c71c2625
2. **Validates compliance** against 28+ specific criteria from Intent Document
3. **Preserves compliant files** to minimize filesystem mutations
4. **Regenerates only non-compliant files** with correct implementations

**Architectural Decision Rationale:**
- Orbit c71c2625 completed successfully (all 6 artifacts present)
- Files exist in repository structure (`index.html`, `src/main.js`, `src/style.css`)
- Current orbit (a6b4c09a) triggered again suggests validation/verification concerns
- Preserve-then-regenerate strategy minimizes unnecessary file churn

### Build System: Vite Zero-Config Mode

**Vite's Role (Unchanged from Orbit c71c2625):**
- Development server serves `index.html` from root
- Resolves bare ES6 imports (`'three'`) to `node_modules/`
- Provides HMR for code changes without full page reload
- No `vite.config.js` required — defaults handle Three.js correctly

**Critical Integration Points:**
- `package.json` contains `"type": "module"` (enables ES6 in Node context)
- HTML script tag uses `type="module"` (enables ES6 imports in browser)
- Three.js 0.160.0+ exports ES6 modules via `"exports"` field

### Runtime Environment: Browser WebGL 2.0 Context

**Execution Flow (No Changes from Orbit c71c2625):**
```
Browser loads index.html
  ↓
Parse HTML → discover <script type="module" src="/src/main.js">
  ↓
Browser fetches main.js from Vite dev server
  ↓
Browser parses ES6 imports → fetches Three.js modules
  ↓
Browser executes main.js:
  - Creates WebGL context from <canvas id="app">
  - Initializes Scene, Camera, Renderer, Geometry, Lights, Controls
  - Starts requestAnimationFrame loop
  ↓
GPU renders frames at ~60fps
  ↓
OrbitControls handles mouse events
  ↓
Window resize events trigger camera/renderer updates
```

### Data Flow: Imperative Render Loop

**Scene Graph Structure:**
```
Scene (root container)
├── PerspectiveCamera (viewing frustum)
├── Mesh (cube)
│   ├── BoxGeometry (vertex data)
│   └── MeshStandardMaterial (PBR surface shader)
├── AmbientLight (uniform illumination)
└── DirectionalLight (directional illumination)
```

**Animation Loop Pattern:**
```javascript
function animate() {
  requestAnimationFrame(animate);
  cube.rotation.x += 0.01;
  cube.rotation.y += 0.01;
  controls.update();
  renderer.render(scene, camera);
}
```

## Pattern Library

### HTML Entry Point Pattern

**Canonical Structure (Expected from Orbit c71c2625):**
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Three.js Starter</title>
  <link rel="stylesheet" href="/src/style.css">
</head>
<body>
  <canvas id="app"></canvas>
  <script type="module" src="/src/main.js"></script>
</body>
</html>
```

**Validation Points:**
- Canvas element has `id="app"` for JavaScript query selector
- CSS loaded in `<head>` before script execution
- Script tag has `type="module"` for ES6 import support
- Paths use root-relative format (`/src/...`) for Vite resolution

### CSS Reset Pattern

**Canonical Structure (Expected from Orbit c71c2625):**
```css
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body {
  width: 100%;
  height: 100%;
  overflow: hidden;
}

#app {
  display: block;
  width: 100%;
  height: 100%;
}
```

**Why Each Rule Matters:**
- `* { margin: 0; padding: 0; }` — Removes browser defaults (especially 8px body margin)
- `overflow: hidden` on `html, body` — Prevents scrollbars if content overflows
- `#app { display: block; }` — Eliminates 4px inline-element descender space
- Full-height chain ensures canvas fills viewport without gaps

### Three.js Initialization Pattern

**Canonical Sequence (Expected from Orbit c71c2625):**

```javascript
import * as THREE from 'three';
import { OrbitControls } from 'three/examples/jsm/controls/OrbitControls.js';

// 1. Scene setup
const scene = new THREE.Scene();
scene.background = new THREE.Color(0x1a1a2e);

// 2. Camera setup
const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
camera.position.z = 5;

// 3. Renderer setup
const renderer = new THREE.WebGLRenderer({ canvas: document.querySelector('#app') });
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.setPixelRatio(window.devicePixelRatio);

// 4. Geometry + Material
const geometry = new THREE.BoxGeometry(1, 1, 1);
const material = new THREE.MeshStandardMaterial({ color: 0x00ff00 });
const cube = new THREE.Mesh(geometry, material);
scene.add(cube);

// 5. Lights
const ambientLight = new THREE.AmbientLight(0xffffff, 0.5);
scene.add(ambientLight);
const directionalLight = new THREE.DirectionalLight(0xffffff, 0.5);
directionalLight.position.set(5, 5, 5);
scene.add(directionalLight);

// 6. Controls
const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;

// 7. Animation loop
function animate() {
  requestAnimationFrame(animate);
  cube.rotation.x += 0.01;
  cube.rotation.y += 0.01;
  controls.update();
  renderer.render(scene, camera);
}
animate();

// 8. Resize handler
window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});
```

**Critical Ordering Dependencies:**
- Renderer must exist before OrbitControls (needs `renderer.domElement`)
- Lights must be added before first render (MeshStandardMaterial requires lights)
- `camera.updateProjectionMatrix()` must be called after changing `camera.aspect`
- `controls.update()` must be called before `renderer.render()` in animation loop

### OrbitControls Import Path (Three.js 0.160.0+)

**Correct Path:**
```javascript
import { OrbitControls } from 'three/examples/jsm/controls/OrbitControls.js';
```

**Breaking Change Context:**
- Three.js < 0.160.0: `/examples/js/controls/OrbitControls` (deprecated)
- Three.js ≥ 0.160.0: `/examples/jsm/controls/OrbitControls.js` (current)
- **Must include `.js` extension** for Vite's ES module resolution

**Validation Failure Signature:**
If orbit c71c2625 used old path, browser console will show:
```
Failed to resolve module specifier "three/examples/js/controls/OrbitControls"
```

## Prior Orbit References

### Orbit c71c2625-0f6b-441c-a24b-a5d187d1ae16 (Previous Attempt)

**Artifacts Generated:**
- `intent_document.md` ✅
- `context_package.md` ✅
- `proposal_record.md` ✅
- `verification_protocol.md` ✅
- `code_generation.md` ✅
- `test_results.md` ✅

**Status:** Complete artifact set (6/6 documents present)

**Repository State After Orbit c71c2625:**
- ✅ `package.json` exists with correct dependencies and scripts
- ✅ `index.html` exists in repository structure
- ✅ `src/main.js` exists in repository structure
- ✅ `src/style.css` exists in repository structure

**Inference:**
Orbit c71c2625 successfully generated all 4 required files. However, current orbit (a6b4c09a) is running again, suggesting one of:
1. **Validation Concerns:** Files may not meet all 28+ acceptance criteria from Intent Document
2. **Human Verification Request:** Explicit request to re-validate implementation quality
3. **Repository State Divergence:** Files were manually edited after orbit c71c2625 completion

**Key Learning:**
Orbit c71c2625 took a generation-first approach. Current orbit should take a validation-first approach:
- Read existing files to determine actual compliance
- Preserve compliant files to avoid unnecessary mutations
- Regenerate only non-compliant files with corrections

### Orbit a6b4c09a-283b-48f1-9386-68f4ef295656 (Current Orbit)

**Artifacts Generated So Far:**
- `intent_document.md` ✅
- `context_package.md` ✅ (this document)
- `proposal_record.md` (next phase)

**Remaining Artifacts:**
- `verification_protocol.md`
- `code_generation.md`
- `test_results.md`

**Unique Approach:**
Unlike orbit c71c2625 (generate everything), this orbit validates before regenerating. This minimizes filesystem churn and preserves working code where possible.

## Risk Assessment

### Risk 1: All Files Already Compliant — Unnecessary Regeneration
**Probability:** Medium  
**Impact:** Low (no functional change, but unnecessary file writes)

**Scenario:**
Orbit c71c2625 generated fully compliant files, but orbit a6b4c09a triggered for verification purposes only.

**Detection Strategy:**
Phase 1 validation will check all 28+ criteria across 3 files. If all pass, no files will be regenerated.

**Mitigation:**
- Proposal Record must include explicit preservation logic
- Code generation phase must document which files were preserved vs. regenerated
- Human supervision will confirm preservation decisions are correct

**Consequence:**
If all files pass validation, orbit completes with 0 file writes (optimal outcome).

---

### Risk 2: Orbit c71c2625 Used Deprecated OrbitControls Path
**Probability:** Medium  
**Impact:** High (scene renders but controls fail, console errors)

**Technical Root Cause:**
Three.js 0.160.0+ moved OrbitControls from `/examples/js/` to `/examples/jsm/`.

**Validation Check:**
Read `src/main.js` and verify import path contains `/jsm/` and `.js` extension:
```javascript
// ✅ Correct
import { OrbitControls } from 'three/examples/jsm/controls/OrbitControls.js';

// ❌ Deprecated (will fail)
import { OrbitControls } from 'three/examples/js/controls/OrbitControls';
```

**Mitigation:**
If validation detects deprecated path, regenerate `src/main.js` with correct import.

**Detection:**
Browser console will show module resolution error if path is incorrect.

---

### Risk 3: CSS Reset Incomplete — Scrollbars Appear
**Probability:** Low  
**Impact:** Medium (violates acceptance criterion, obvious visual defect)

**Validation Checks:**
- [ ] Universal selector reset present (`* { margin: 0; padding: 0; box-sizing: border-box; }`)
- [ ] Full-height chain present (`html, body { width: 100%; height: 100%; overflow: hidden; }`)
- [ ] Canvas display override present (`#app { display: block; width: 100%; height: 100%; }`)

**Common Failure Modes:**

| Missing Rule | Visual Bug |
|--------------|------------|
| `* { margin: 0; }` | 8px white border around canvas |
| `overflow: hidden` | 1-2px scrollable area triggers scrollbars |
| `#app { display: block; }` | 4px vertical gap below canvas |

**Mitigation:**
If any CSS rule is missing or incorrect, regenerate `src/style.css` with canonical implementation.

**Detection:**
Visual inspection: any scrollbar (horizontal or vertical) indicates CSS reset failure.

---

### Risk 4: Line Count Exceeds 100 Lines in `src/main.js`
**Probability:** Low (orbit c71c2625 likely followed same constraint)  
**Impact:** Low (violates code quality gate)

**Validation Check:**
```bash
wc -l src/main.js
```
Expected: ≤ 100 lines

**Mitigation:**
If line count exceeds 100, regenerate with compact style:
- No comments
- Minimal whitespace (1 blank line between sections)
- No helper functions (inline all logic)

**Current Estimate:**
Canonical implementation is ~40 lines (60% headroom), so violation is unlikely unless orbit c71c2625 added verbose comments or helpers.

---

### Risk 5: MeshStandardMaterial Without Lights
**Probability:** Very Low (lights explicitly required in Intent)  
**Impact:** High (cube renders black, looks like rendering failure)

**Validation Check:**
Verify `src/main.js` contains:
```javascript
const ambientLight = new THREE.AmbientLight(0xffffff, 0.5);
scene.add(ambientLight);
const directionalLight = new THREE.DirectionalLight(0xffffff, 0.5);
directionalLight.position.set(5, 5, 5);
scene.add(directionalLight);
```

**Failure Signature:**
Cube appears as black silhouette instead of lit green cube with shading gradients.

**Mitigation:**
If lights are missing or not added to scene, regenerate `src/main.js` with correct lighting setup.

---

### Risk 6: Missing `camera.updateProjectionMatrix()` in Resize Handler
**Probability:** Low  
**Impact:** High (cube distorts on window resize)

**Validation Check:**
Verify resize handler contains all 3 required lines:
```javascript
window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();  // Critical line
  renderer.setSize(window.innerWidth, window.innerHeight);
});
```

**Failure Signature:**
Resize browser window → cube stretches into rectangular prism instead of maintaining 1:1:1 proportions.

**Mitigation:**
If `updateProjectionMatrix()` is missing, regenerate `src/main.js` with correct resize handler.

---

### Risk 7: Missing `controls.update()` in Animation Loop
**Probability:** Low  
**Impact:** Medium (controls feel jerky instead of smooth)

**Validation Check:**
Verify animation loop calls `controls.update()` before `renderer.render()`:
```javascript
function animate() {
  requestAnimationFrame(animate);
  cube.rotation.x += 0.01;
  cube.rotation.y += 0.01;
  controls.update();              // Required for damping
  renderer.render(scene, camera);
}
```

**Failure Signature:**
Camera movement feels "snappy" or "sticky" instead of smooth interpolation.

**Mitigation:**
If `controls.update()` is missing, regenerate `src/main.js` with correct animation loop.

---

### Risk 8: File Preservation Logic Error
**Probability:** Low  
**Impact:** Medium (compliant files unnecessarily regenerated)

**Scenario:**
Validation logic has bug and incorrectly marks compliant file as non-compliant, triggering unnecessary regeneration.

**Mitigation:**
- Proposal Record must document exact validation criteria for each file
- Code generation phase must log validation results before regeneration
- Human supervision reviews validation decisions in pre-execution review

**Detection:**
Human compares generated files against orbit c71c2625 files to confirm only necessary changes were made.